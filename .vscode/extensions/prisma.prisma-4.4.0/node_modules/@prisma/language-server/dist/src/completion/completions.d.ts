import { CompletionList, Position } from 'vscode-languageserver';
import type { TextDocument } from 'vscode-languageserver-textdocument';
import { Block, BlockType } from '../util';
export declare function getSuggestionForNativeTypes(foundBlock: Block, lines: string[], wordsBeforePosition: string[], document: TextDocument): CompletionList | undefined;
export declare function getSuggestionForFieldAttribute(block: Block, currentLine: string, lines: string[], wordsBeforePosition: string[], document: TextDocument): CompletionList | undefined;
export declare function getSuggestionsForFieldTypes(foundBlock: Block, lines: string[], position: Position, currentLineUntrimmed: string): CompletionList;
/**
 * gets suggestions for block type
 */
export declare function getSuggestionForFirstInsideBlock(blockType: BlockType, lines: string[], position: Position, block: Block): CompletionList;
export declare function getSuggestionForBlockTypes(lines: string[]): CompletionList;
export declare function suggestEqualSymbol(blockType: BlockType): CompletionList | undefined;
export declare function getSuggestionForSupportedFields(blockType: BlockType, currentLine: string, currentLineUntrimmed: string, position: Position, lines: string[]): CompletionList | undefined;
export declare function getSuggestionsForInsideRoundBrackets(untrimmedCurrentLine: string, lines: string[], document: TextDocument, position: Position, block: Block): CompletionList | undefined;
